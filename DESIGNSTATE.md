# DESIGNSTATE.md — Sift (ScreenSort-LAM) App Redesign (v3 — fresh start)

## Request
Fully redesign the whole Sift app. **Instruction source: https://github.com/Meliwat/awesome-ios-design-md** (200 production-grade iOS DESIGN.md design systems). Erase the former designs and make a new one driven by that instruction set.

**NOTE — v2 abandoned at user request (2026-08-09).** The previous direction ("Ember & Porcelain": cool porcelain light / warm charcoal dark, vermilion ember accent, Roboto/SF only) is fully discarded — palette, type system, mark, all gone. This file restarts the pipeline from scratch. No visual decisions from v1 or v2 carry over. Only the factual feature inventory below persists.

## Binding instruction set (from the repo)
- **Primary design DNA = Claude** (`design-md/misc/claude/DESIGN.md`, framework-neutral): warm cream paper canvas, inky warm text, single terracotta Claude Orange accent, serif assistant body / sans user+chrome typographic split, asterisk-star logomark, no gradients/no glass/no glow, warm dark mode. Full spec fetched; the taste stage adapts it into Flutter tokens.
- **Secondary pattern = Perplexity** (`design-md/misc/perplexity/DESIGN.md`): the grounded-evidence pattern — horizontal source-card row + inline citation chips above every answer. Maps 1:1 onto Sift's "Read from N screenshots" recall evidence.
- The repo's own font fallback rules apply: Tiempos → **Source Serif 4** (OFL, closest free substitute; doc explicitly names it), Styrene → platform sans (system stack / Inter substitute).

## Feature inventory (facts only — what the app does)
- **Sift**: Flutter app. Screenshot → ML Kit OCR → Gemini/LAM multimodal understanding (description, objects, recognitions, extracted text, summary, type, confidence) → actions (calendar, reminder, shopping list, task). Local-first Hive storage. Differentiator: **conversational recall** — ask about past screenshots in plain language; answers grounded in saved screenshots.
- Existing screens: Home (header, stats, list, empty state, FAB Scan, processing/error banners), Detail (hero image, "What SIFT sees", action card, extracted text, info), Chat "Ask SIFT", Settings (AI providers + API keys, appearance, behavior), Onboarding (3 pages), Actions History, Shopping List.
- Known product bugs fixed pre-v3 (do not regress): ThemeController real (not decorative) theme switch; `fontFamily: 'Inter'` was a lie → new type system replaces it; watcher hoisted; chat errors sanitized; Android backup disabled.
- Tech constraints: Material 3, Flutter SDK ≥3.29 (actual: 3.44.6 / Dart 3.12.2), deps: provider, hive, shared_preferences, intl, image_picker, uuid, http, google_mlkit_text_recognition, flutter_local_notifications, add_2_calendar, timezone, permission_handler. **No new pub deps unless truly required** (font assets are NOT deps — bundling .ttf is allowed).
- Security facts: API keys in SharedPreferences; keys were previously leaked in WORKFLOWSTATE.md (rotated by user); no secrets in plaintext; sanitize any provider errors (they can embed keys in URIs).

## Plan (pipeline)
1. **product** — re-derive intent under Claude DNA (audience, JTBD, message, content, requirements). Evolve v2 product decisions only where the new DNA demands (do not carry over visual choices).
2. **structure** — IA/layout: sections, hierarchy, page flow, responsive order under the new language.
3. **taste** — adapt Claude DESIGN.md → complete Flutter token system (hex ramp, serif/sans type scale, components, radii, elevation, do/don'ts) + Perplexity evidence pattern. THE core stage.
4. **knowledge** — validate: font bundling feasibility (Source Serif 4 OFL download), M3 ColorScheme mapping, WCAG contrast, no-new-deps, conflicts with existing code (emoji, gradients, badges, mark).
5. **motion** — motion language (Claude send scale, pill slide, streaming cursor, thinking pulse; tab transitions; reduced-motion).
6. Consolidate all five into one buildable spec (no conflicts).
7. implementor → reviewer → security-reviewer → tester → linter → commit-message.

## Product Direction (v3 — Claude DNA)
- **One-liner**: "Sift is your phone's quiet memory — a warm, private assistant that answers in plain words from the screenshots you actually saved."
- **Persona**: Maya, 31, PM who screenshots everything; one hand, 15–90s, at-the-moment-of-need sessions; aesthetics-sensitive; demands proof (every answer visibly grounded).
- **3 emotion keywords**: Warm (paper, terracotta, handled-not-clinical) / Essayist (recall is told in composed prose; serif is the signature) / Private (calm ownership: "Everything lives on this device." — no fear, no theater).
- **Voice guardrails**: sentence case everywhere; no exclamation marks; no emoji in UI; no "AI-powered/smart/magic" language; UI says "you", only assistant may say "I" sparingly; numbers in prose not badges.
- **Microcopy (binding)**: Ask empty headline "What would you like to remember?"; composer "Ask about anything you've saved…"; 4 example prompts (recipes / Lisbon flight / AirBnB Wi-Fi / deadlines & bills); typing "Looking through your screenshots…"; evidence line "Read from N screenshots" (singular "Read from 1 screenshot"); never answer without evidence — explicit "couldn't find that" + closest match instead; low-confidence labeled "partial match"; onboarding beats: "Your screenshots, remembered." / "Ask in plain words." / "Quietly private." with final CTA "Start sifting" landing on Settings' setup checklist (not Home); Settings header "Get Sift ready" 3-step checklist (Choose a provider / Add your API key / Enable auto-detect) with calm per-step copy; privacy lines per screen ("Your screenshots stay on this device." etc.).
- **Surviving product conclusions (unchanged)**: Library image-first + persistent Ask input; Chat = first-class tab; Detail image-first; Settings = setup checklist; evidence row = trust backbone; Home stats → one quiet sentence ("12 screenshots remembered"), never a dashboard.
- **Anti-goals**: no gradients/glass/glow/decorative shadows; no robot/sparkle/rocket/brain icons (asterisk-star mark only, Material rounded-outline icons); no AI-badge language; no cold blue-grey; no pure-black text; no per-provider colors; no stats wall; no ungrounded answers; no ALL-CAPS/letter-spaced labels; no emoji; don't regress fixed bugs (no fake Inter, watcher hoisted, real ThemeController, sanitized errors, backup disabled).

## Structure Plan (v3 — decided)
- **Navigation**: Claude-styled flat bottom nav, 3 tabs Library/Ask/More, NO floating capsule, NO shadow — edge-to-edge paper + hairline top only. Active = terracotta icon + ink label (color only, no indicator container, no badge/count). IndexedStack shell kept. No FAB. Capture = camera circle inside Library's pinned Ask bar. Actions History = Library header icon + a "Library" cluster row in More. No Ask input in More.
- **Global rhythm**: 4pt base, 20pt gutters, 32–48pt sections, 32pt chat turns; radius lock: cards 20, fields/buttons 16, thumbs 12, chips/pills full; nav bar radius 0. Serif = assistant voice (AI-written strings, summaries, chat answers, headlines); sans = all chrome. Tabular figures for dates/numerals; mono ONLY for OCR block.
- **Library**: pinned flat Ask bar (field + terracotta camera circle) → wordmark `Sift` + ONE quiet sentence (only stats surface) + pin/history icons → banners (flat) → time groups (Today/Yesterday/Earlier, small-caps sans, NO count badges) → 16:9 image-forward cards (image + time chip + pin toggle; serif 2-line summary; type badge + ≤3 tags) → empty states (serif headline) → 96pt bottom inset. Tablet ≥600pt 2-col grid, ≥1000pt 3-col.
- **Ask**: hero = serif headline + input + 4 example chips + Recent + privacy footnote. Conversation: user soft pill → **evidence strip ABOVE every answer** (48pt square thumbs r12 + "Read from N screenshots", N in terracotta tabular) → **assistant flat essay block** (serif, no container, left-aligned, full width, 32pt turn spacing) → flat typing row. Composer docked above nav (flat field + terracotta send). Tablet: conversation column centered max 680–720pt.
- **More**: single ListView, zero bordered cards. `More` headline → Library cluster (Actions history) → **"Get Sift ready" checklist** (1 Choose provider / 2 Add key / 3 Enable auto-detect; step numerals = status glyph, terracotta when done; progressive disclosure inline, no nested pages; YouTube key merges into step 2) → Appearance (segmented theme, quiet) → Behavior (haptics) → Privacy (local-only + 3 flat info rows + delete) → About (asterisk mark + version).
- **Onboarding**: 3 beats (Promise serif headline / Exchange with REAL chat components — user pill + evidence strip + serif essay line in a phone frame / Privacy + 3 flat trust rows), 3 quiet dots (active = terracotta), Skip top-right, final CTA "Set up Sift" → deep-links to More tab with checklist scrolled into view (shell must accept initial index + scroll target).
- **Detail**: hero sliver ~240pt full-bleed + back/pin/share quiet circles → type badge + status → **serif summary** → "What Sift sees" label + serif description (flat, no container) + recognition chips → extracted data rows (hairline) → Found online / Find online → Tags editor → OCR mono block (quiet, selectable) → Details rows. Action bar: pinned bottom paper bar on phone (primary FilledButton + Share); side rail ~260pt sticky at ≥840pt.
- **Actions History + Shopping**: quiet headers, de-containerized hairline rows, filter chips sentence case, empty serif state; Shopping = one quiet sentence progress ("2 of 5 items", NO %/bar), checkbox rows with line-through.
- **Do/don'ts**: one accent only; paper default; evidence strip mandatory above every answer; no stat walls; no card-soup; no gradients/glass/glow; no AI-badge language; sentence case; no exclamation/emoji; no FAB; no dividers between turns.
- **Build order**: 1) tokens+mark 2) shell+nav 3) conversation atoms (pill/strip/essay/typing) 4) Ask 5) Library 6) More 7) Detail 8) Onboarding 9) History+Shopping.

## Taste Direction (v3 — decided)
- **One-liner**: "Warm paper recall — Sift is a chat with your screenshot memory, typeset like a letterpress essay and grounded in terracotta-proofed thumbnails on cream paper."
- **Palette (light)**: canvas #F8F4ED, paper #FBF9F4, surfaceWarm1 #F0EAE0, surfaceWarm2 #E8E0D2, divider #DDD2BD; ink #2D2520, graphite #5A4F44, stone #8A7E72, bone #B5AB9E; accent #D97757 (unchanged dark), accentPressed #BE6242, accentSoft #F2DDD0, onAccent #FBF9F4; success #6B9D5E, warning #D49952, error #C16654, errorSoft #F3E0DC, info #6E6A62; codeBg #1F1B16, codeText #E8E0D2, codeBorder #DDD2BD; badgeBg #E8E0D2/badgeText #5A4F44, tagBorder #DDD2BD/tagText #8A7E72; scrimPhoto #8C1F1B16, barrier #662D2520; warm shadows rgba(40,30,20,x): L1 0.04 (0,1)/2, L2 0.06 (0,2)/8, L3 0.10 (0,4)/16, L4 0.14 (0,8)/24, L5 0.18 (0,-12)/32, L4Dark 0.08; focus ring 1.5pt accent.
- **Palette (dark)**: canvas #1F1B16, paper #2A2520, surfaceWarm1 #2A2520, surfaceWarm2 #3A332C, divider #3A332C; ink #E8E0D2, graphite #B5AB9E, stone #8A7E72, bone #5A4F44; accentSoft #4A352A, errorSoft #4A2E28; success #7FB069, error #D9846F, info #A49E93; codeBg #161210; shadows eliminated below L4 (hairlines separate instead).
- **Typography**: serif = Source Serif 4 (bundled assets: Regular 400, Italic 400, SemiBold 600; OFL) — assistant voice + essayist headlines (display 32/600/-0.4/1.2, title 28, headline 24, subhead 20, subhead2 17, body 16/400/**1.55** — never below 16pt, summary 17); sans = system stack (NEVER declare Inter — that was the v2 fake-font bug) — body 16, bodyMd 15, chromeTitle 18/600, button 15/600, chip 13/500, meta 12, micro 11, tab 11/500; mono = JetBrains Mono 400 (bundled, OCR block ONLY, 13/1.6). Tabular figures on all numeric/meta roles.
- **Components (key decisions)**: flat bottom nav (custom widget, not M3 NavigationBar — canvas bg, 1pt hairline top, active = accent icon + ink label color-only, no capsule/badge); Ask bar/composer (paper field r16, 1pt divider border → 1.5pt accent focus, h52, camera circle always accent in Library / send circle accent-when-text in Ask); user soft pill (surfaceWarm1, capsule, right, max 80%); assistant essay block (FLAT, no container, sender row mark 16 + "Sift" graphite + time, serifBody, word-by-word streaming + orange caret, "Regenerate" outlined + copy); **evidence strip mandatory above every answer** (48px square thumbs r12 hairline + "+N" overflow square, caption "Read from N screenshots" with N in accent 600 tnum, " · partial match" warning suffix; strip never renders without answer); typing row = pulsing mark 1.0→1.15 1200ms + "Looking through your screenshots…" (no pill, no dots); chips full capsule surfaceWarm1; screenshot card r20 paper hairline L1, 16:9 image top clipped, time chip + pin toggle on scrimPhoto, serifSummary 2-line, neutral type badge + ≤3 tags; settings flat rows (zero cards) + checklist step numerals (28pt circle: warm2+ink numeral pending / accentSoft+accent check done); segmented theme control surfaceWarm1 container r16, selected = paper r13; **primary FilledButton = INK-filled r16** (orange stays sacred for send/active/mark/links), dark flips to cream-fill; outlined = paper+divider r16; OCR block warm dark r12 + copy button; snackbar floating ink-filled; sheet r24 top + L4; dialog r20; empty states = mark 40 + serif headline; skeletons = static warm blocks (NO shimmer); banners flat (processing surfaceWarm2 + pulsing mark / error errorSoft).
- **The mark**: `SiftMarkPainter` CustomPainter — 6-petal asterisk-meets-star (petals at −90° + k·60°, tip R=0.45s, width W=0.16s, round caps, hub circle 0.09s; high-fidelity filled quadratic variant at splash 56/onboarding 80). NEVER Icons.auto_awesome / Icons.asterisk. Sizes: splash 56, onboarding 80, chat avatar 16, typing 14, about 40, empty 40, checklist done = check icon (not mark).
- **Do/don'ts (20 hard rules)**: cream canvas everywhere; ONE accent as verb; serif-assistant/sans-chrome asymmetry; no gradients/glass/glow; no pure black; no cool greys; no per-provider colors; exact radius lock (0/4/12/16/20/24/full/circle); warm shadows L1-L2 only, dark = hairlines; no bubble tails; no nav containers/badges; no FAB; no emoji/exclamation/ALL-CAPS; no mono except OCR; no sparkle icons; evidence strip mandatory; numbers in prose; no typewriter (word-by-word 80ms); no shimmer; serif ≥16pt; only bundled/system fonts.
- **M3 construction**: build ColorScheme BY HAND from tokens (fromSeed BANNED — tonal tinting breaks flat paper); primary=accent, onPrimary=onAccent, primaryContainer=accentSoft, surface=canvas, outline=divider, surfaceTintColor transparent; custom components for nav/chips/inputs.
- **Conflicts resolved**: doc's "no tab bar" → styling discipline kept, topology adapted; doc ink-capsule primary → ink r16; user pill r18 → full capsule; doc divider between turns → whitespace; input r20/action r10 → 16 lock; model chip → provider checklist numerals; cool Info Slate → warm #6E6A62; code mono → OCR-only (inline code = sans on tint); doc serif screen titles → serif headlines ARE essayist voice; thinking pill → flat typing row; M3 defaults overridden by hand-built scheme.

## Knowledge Notes (v3 — validated, GO with binding ADJUSTs)
- **Stack**: Flutter 3.44.6 / Dart 3.12.2 verified against installed SDK. Hand-built `ColorScheme` valid; param is **`surfaceTint:`** (not surfaceTintColor); only brightness/primary/onPrimary required. `ThemeExtension<SiftColors>` confirmed (`Theme.of(context).extension<SiftColors>()`); needs copyWith+lerp.
- **Fonts — VARIABLE, not static**: google/fonts ships Source Serif 4 + JetBrains Mono as variable TTFs only (statics don't exist anywhere reliable; Newsreader/Lora also variable-only). Flutter 3.44 maps FontWeight→wght axis natively (engine PR #175771, breaking-change doc May 2026) — no synthetic bold. **Bundle 3 files**: `SourceSerif4[opsz,wght].ttf` (https://raw.githubusercontent.com/google/fonts/main/ofl/sourceserif4/SourceSerif4%5Bopsz%2Cwght%5D.ttf), `SourceSerif4-Italic[opsz,wght].ttf` (…/SourceSerif4-Italic%5Bopsz%2Cwght%5D.ttf), `JetBrainsMono[wght].ttf` (…/ofl/jetbrainsmono/JetBrainsMono%5Bwght%5D.ttf) + 2 OFL.txt. Rename bracket-free (SourceSerif4VF.ttf, SourceSerif4-ItalicVF.ttf, JetBrainsMonoVF.ttf). pubspec `flutter: fonts:` block; create `assets/fonts/`; add **LicenseRegistry** in main() (currently ZERO license handling). Add `FontVariation('opsz', 30)` on serif display roles ≥28px (serifDisplay 32, serifTitle 28). ~2.2MB bundle cost accepted.
- **WCAG fixes (BINDING — override taste hexes)**: NEW token **accentDeep #B04F2B** — interactive fills (send/camera/FilledButton fill, nav active count/label), links in light mode (4.79), white-text-on-fill pairs (onAccent #FBF9F4 on accentDeep = 4.99 ✓; onAccent on #D97757 = 2.97 ✗ FAILS — never white text on raw #D97757). Keep #D97757 for: mark, decorative/large, dark-mode links (5.48 ✓). Semantic light: success **#4E7C43**, warning **#96600F**, error **#A64A33**; stone→**#7A6E61** (small text), tagText→#7A6E61; dark stone→**#998D80** (spec's 4.6 claim was actually 4.33). Dark: success #7FB069, error #D9846F, warning #D49952, info #A49E93 all pass. **ScrimPhoto alpha 0x8C→0xAA** (67%, time chip + pin + back sit behind small text). OCR `'monospace'`→`'JetBrainsMono'` (detail_screen.dart:279, keep SelectableText).
- **Codebase is clean** (verified): 0 gradients, 0 emoji, 0 auto_awesome usages, 0 Inter, 0 hardcoded hexes outside theme (all 60 hexes in app_theme.dart), 0 LicenseRegistry. `fromSeed` at app_theme.dart:111/142 → hand-built. sift_mark.dart (3 stacked bars, emberMain default) → CustomPainter; call sites: main.dart:141 splash 56px, about_dialog.dart:31 (40px), widgets.dart:170 ProcessingBanner (18px), widgets.dart:232 EmptyState (48px). app_shell.dart:56-96 floating capsule nav → flat paper bar; ~25 `AppTheme.emberMain` refs across 10 files → accent retokenization; widget_test.dart is placeholder (1+1==2) — cannot break. ThemeController needs zero changes (tokens flow via AppTheme getters).
- **Open questions resolved**: (1) Button text = onAccent #FBF9F4 on **accentDeep #B04F2B** (ink-on-terracotta not viable cross-mode). (2) Dark semantic final: #7FB069/#D9846F/#D49952/#A49E93. (3) ScrimPhoto IS behind small text (time chip 11pt) → alpha 0xAA. (4) opsz variation applied on ≥28px serif display.

## Motion Plan (v3 — decided)
- **Principles**: "a quiet amanuensis — confirms, never performs." Settle ≤400ms (500 celebration), press ≤80ms, one-shot entrances, no scroll-triggered per-item anims, no Hero, no shimmer. Loops only: mark pulse + streaming caret (reduced-motion gated).
- **Tokens**: press 60ms easeOutCubic, pressRelease 140ms, standard 200ms, emphasis 300ms, stagger 60ms, wordTick 80ms, caretBlink 600ms step, markPulse 1200ms TweenSequence (1.0→1.15 easeOutCubic 45% / easeInCubic 55%), celebration 500ms, dismiss 150ms easeInCubic, pulseOvershoot easeOutBack amplitude ≤1.06 (mark pulse ONLY). `MotionTokens` abstract final class in lib/theme/motion_tokens.dart; `reduced` + `canHaptic` getters from MediaQuery.disableAnimationsOf.
- **Micro**: nav active = color TweenAnimationBuilder 200ms (icon+label lerp to accent, no container/scale); send/camera press scale 0.94 60ms→1.0 140ms + mediumImpact once; focus ring AnimatedContainer border 200ms; user pill slide translateY(16)→0 + fade 300ms + composer clears 150ms + jumpTo bottom; evidence strip 350ms rise + thumbs stagger 60ms (220ms each) + caption at +60ms; typing row mark pulse + min display 600ms; streaming caret blink 600ms reset-to-visible on word tick, fade 200ms at end; word streaming = ONE Text.rich + Timer.periodic(80ms) + single trailing-word controller (NO per-word widgets, NO typewriter); copy morph 150ms + toast 200ms/1200ms dismiss; checklist AnimatedSize 250ms + content fade 200ms + check scale 0.8→1.0; segmented thumb 200ms + selectionChanged; card→Detail = PageRouteBuilder fade+rise 300ms (NO Hero); clear chat / empty↔conversation AnimatedSwitcher 250ms; chip tap press 60ms + lightImpact.
- **Page**: onboarding PageView default slide + dots color-only AnimatedContainer 200ms (no morph/width); "Set up Sift" deep-link = tab switch + scroll-to-checklist 300ms; sheet rise 300ms (AnimationStyle) + scrim 200ms; dialog fade+scale 0.96→1.0 200ms; Detail back pop 250ms reverse; Ask bar elevation via NotificationListener >8px → AnimatedContainer 180ms paint-only; IndexedStack NO cross-fade between tabs (absence of motion is the motion).
- **Loading**: skeleton static blocks fade in 200ms/fade out 150ms (no shimmer); processing banner slide-in 200ms + pulsing mark; error banner 200ms; typing→reply min 600ms then strip cascade then stream.
- **Haptics (reduced-motion gated)**: send/capture mediumImpact once; chip tap lightImpact; action executed/destructive confirm mediumImpact once; tab switch selectionChanged (never during scroll); shopping checkbox lightImpact; copy/scroll/nav none.
- **Reduced-motion**: all durations zero, curves linear, loops stop, haptics skip; word stream renders at once; caret static; pulse static; routes instant; PageView slide preserved (navigation, not decoration).
- **Do/don'ts (10)**: transforms+opacity only; one-shot entrances; no bounce ≥56pt; no idle loops except pulse/caret; press ≤80ms + settle ≤400ms; no Hero; no shimmer; haptics sparse; stagger only evidence thumbs (cards one-shot, quiet on scroll); implicit widgets for one-shots, explicit controllers only for pulse/caret/word timer, dispose every timer.

## Consolidated Design Direction (v3 — "Warm Paper Recall")

**One line**: Sift becomes a warm, private second memory for your screenshots — cream paper canvas, one terracotta accent, assistant answers typeset in serif like an essayist's letter, every answer proven by a thumbnail evidence strip, everything calm and local-first.

### 1. Architecture & new files
- **`lib/theme/app_theme.dart`** (rewrite): hand-built `ColorScheme` (NO fromSeed) with `surfaceTint: Colors.transparent`; `SiftColors` as `ThemeExtension<SiftColors>` (light/dark, copyWith+lerp); `SiftRadii`/`SiftSpacing`/`SiftElevation` constants; `SiftType` text theme (serif roles fontFamily 'SourceSerif4', sans roles no family override); OCR role uses 'JetBrainsMono'. `AppTheme.lightTheme`/`darkTheme` getters keep their signatures (ThemeController unchanged).
- **`lib/theme/motion_tokens.dart`** (new): abstract final MotionTokens (durations, curves incl. EaseOutBackCapped 1.06, `reduced`, `canHaptic`).
- **`lib/widgets/sift_mark.dart`** (rewrite): `SiftMarkPainter` CustomPainter — 6-petal asterisk-star (petals −90°+k·60°, tip R=0.45s, width W=0.16s, round caps, hub circle 0.09s; high-fidelity filled quadratic variant for splash/onboarding). `SiftMark` widget keeps `{required double size, Color? color}` API (call sites: splash 56, onboarding 80, chat avatar 16, typing 14, about 40, empty 40). NEVER auto_awesome/asterisk.
- **`lib/screens/app_shell.dart`** (rewrite of nav): flat paper bottom nav (custom widget, NOT M3 NavigationBar): canvas bg, 0.5pt(light)/1pt(dark) hairline top, height 64+safe, 3 items Library(Icons.photo_rounded)/Ask(Icons.chat_bubble_outline_rounded)/More(Icons.tune_rounded), active = accent icon + ink label (color-only 200ms), inactive stone, no capsule/badge/shadow. Keep IndexedStack. Add deep-link support: initial tab index + scroll target for onboarding "Set up Sift" → More checklist.
- **`assets/fonts/`** (new): download 3 variable TTFs + 2 OFL.txt (URLs in Knowledge Notes). pubspec `flutter: fonts:` block (SourceSerif4 + JetBrainsMono families). `LicenseRegistry.addLicense` in main().
- **`lib/main.dart`**: add LicenseRegistry; fonts; everything else unchanged (ThemeController wiring stays).

### 2. Theme tokens (BINDING — knowledge-corrected)
- Light: canvas #F8F4ED, paper #FBF9F4, surfaceWarm1 #F0EAE0, surfaceWarm2 #E8E0D2, divider #DDD2BD; ink #2D2520, graphite #5A4F44, stone #7A6E61, bone #B5AB9E; accent #D97757, accentDeep #B04F2B, accentPressed #BE6242, accentSoft #F2DDD0, onAccent #FBF9F4; success #4E7C43, warning #96600F, error #A64A33, errorSoft #F3E0DC, info #6E6A62; codeBg #1F1B16, codeText #E8E0D2, codeBorder #DDD2BD; badgeBg #E8E0D2/badgeText #5A4F44, tagBorder #DDD2BD/tagText #7A6E61; scrimPhoto #AA1F1B16 (67%); barrier #662D2520; shadows warm rgba(40,30,20): L1 #0A281E14 (0,1)/2, L2 #0F281E14 (0,2)/8, L3 #1A281E14 (0,4)/16, L4 #24281E14 (0,8)/24, L5 #2E281E14 (0,-12)/32.
- Dark: canvas #1F1B16, paper #2A2520, surfaceWarm1 #2A2520, surfaceWarm2 #3A332C, divider #3A332C; ink #E8E0D2, graphite #B5AB9E, stone #998D80, bone #5A4F44; accent #D97757, accentDeep #B04F2B (fills), accentPressed #BE6242, accentSoft #4A352A, onAccent #FBF9F4; success #7FB069, warning #D49952, error #D9846F, errorSoft #4A2E28, info #A49E93; codeBg #161210; shadows: L4Dark #15281E14 only (sheets); hairlines separate everything else.
- ColorScheme: primary=accentDeep? NO — primary=accent #D97757, onPrimary=onAccent, primaryContainer=accentSoft, onPrimaryContainer=ink; surface=canvas, onSurface=ink, surfaceContainerLow=paper, surfaceContainer=surfaceWarm1, surfaceContainerHigh=surfaceWarm2, onSurfaceVariant=graphite, outline=divider, outlineVariant=divider, inverseSurface=ink/onInverseSurface=paper, error/onError per semantic, surfaceTint: Colors.transparent. Interactive fills (FilledButton bg, send/camera fill, count labels) use **accentDeep** explicitly (contrast); brand/decorative/large + dark links use accent.
- Type: serif (SourceSerif4): serifDisplay 32/600/-0.4/1.2 (+opsz 30), serifTitle 28/600/-0.3/1.25 (+opsz 30), serifHeadline 24/600/-0.2/1.3, serifSubhead 20/600/-0.1/1.3, serifSubhead2 17/600/1.35, serifBody 16/400/1.55 (NEVER below 16), serifSummary 17/400/1.5. Sans (system): bodySans 16/400/1.5, bodySansMd 15/400/1.45, chromeTitle 18/600/-0.1/1.25, buttonLabel 15/600/1.0, chipLabel 13/500/1.2, metaLabel 12/400/1.3, microLabel 11/400/1.3, tabLabel 11/500/1.2. Mono (JetBrainsMono): ocrMono 13/400/1.6. Tabular figures on meta/micro/time/steps/version/evidence-N/data values. NO 'Inter' ever.
- Radii: 0 nav/hairlines, 4 inline code, 12 thumbs/OCR/banners/snackbar, 16 fields/buttons/segment container, 20 cards/dialogs, 24 sheet top, full pills/chips/tags, circle send/dots/mark.
- Buttons: primary FilledButton = **accentDeep fill + onAccent text** r16 h48 (dark: ink #E8E0D2 fill + #1F1B16 text, pressed #C9BFA9); outlined = paper + divider border r16; text = ink (destructive error); focus ring 1.5pt accent.
- Skeleton: static surfaceWarm2 blocks, no shimmer.

### 3. Screen specs (structure + taste decided; verbatim contract)
See Structure Plan + Taste Direction above — they are the binding per-screen contract (Library slivers/Ask two-phase+evidence/More checklist/Onboarding 3 beats/Detail hero+action bar/History+Shopping restyle). Implementor must follow the Build Order: tokens+mark → shell+nav → conversation atoms → Ask → Library → More → Detail → Onboarding → History/Shopping.

### 4. Motion (decided)
See Motion Plan above. Reduced-motion gate in MaterialApp.builder is the single enforcement point.

### 5. Voice & copy (decided)
See Product Direction microcopy (sentence case, no exclamation, no emoji, no AI-badge language; "Read from N screenshots" with N in accent; "Looking through your screenshots…"; onboarding "Set up Sift" CTA → More checklist).

### 6. Explicit non-goals
No new pub deps (font assets allowed). No Hive/schema/provider-logic changes beyond existing fixed behavior. No gradients/glass/glow. No emoji. No per-provider colors. No stats wall. No FAB (camera circle only). No Hero. No shimmer. No typewriter. No auto_awesome/sparkle icons. No fake Inter. No fromSeed. No mono except OCR. No bubbles with tails. No capsule nav. No exclamation marks. No uppercase/labels.

### 7. Known implementation facts (from knowledge)
- flutter analyze currently clean; widget_test.dart placeholder (1+1==2) — keep passing.
- 60 hex literals all inside app_theme.dart; ~25 emberMain refs across 10 files → retokenize.
- OCR font swap at detail_screen.dart:279.
- sift_mark call sites: main.dart:141 (56), about_dialog.dart:31 (40), widgets.dart:170 (18 ProcessingBanner), widgets.dart:232 (48 EmptyState).
- LicenseRegistry currently absent — add in main().
- No assets/ dir exists; create assets/fonts/.

## Implementation Notes
- v3 "Warm Paper Recall" implemented. Changed: pubspec (fonts), main.dart (LicenseRegistry + wiring), lib/theme/app_theme.dart (hand-built ColorScheme, SiftColors ThemeExtension, SiftType serif/sans/mono, motion tokens file), lib/widgets/sift_mark.dart (CustomPainter asterisk-star), lib/widgets/chat_atoms.dart (NEW: user pill, essay block, evidence strip, typing row), widgets/skeleton/bottom_sheet/about_dialog/privacy_gate restyled, app_shell flat nav + deep-link, chat two-phase + evidence strip + streaming, home pinned ask bar + image cards + time groups, settings checklist, onboarding 3 beats, detail hero + honest action bar, actions_history + shopping restyled + wired.
- Fonts bundled (variable): SourceSerif4VF.ttf 1.21MB, SourceSerif4-ItalicVF.ttf 0.85MB, JetBrainsMonoVF.ttf 0.19MB + 2 OFL.txt; LicenseRegistry added.
- Orchestrator decisions applied: no-op Share affordances REMOVED (no plugin, no new dep); ShoppingListScreen wired into Actions History ("View shopping list" flat row when shopping actions exist, null-safe); Detail bar shows "No suggested action" (stone, no tap target) when no action.
- `flutter analyze` → No issues found! · `flutter test` → 23/23 pass. Nothing committed (user decision pending).

## Review Findings
- **Reviewer: PASS-WITH-NOTES → all routed fixes applied (round 2)**. Fixed: (1) More-tab scroll hijack gated to deep-link only (`_checklistRevealed` consumed once); (2) SafeArea(bottom:false) on Chat + Settings roots (edge-to-edge status bar overlap); (3) sanitized process-error banner (raw exceptions could embed keys) → generic "Couldn't analyze this screenshot. Check your API key and try again." + debugPrint raw; (4) evidence strip source ids persisted in ChatMessage (`sourceIds`, tolerant `_stringList` parse — old messages load with no strip, never crash; rebuilt from provider.screenshots on load, stale ids skipped); (5) dark-mode ask-bar shadow gated; (6) splash pulse → MotionTokens.pulseCycle; (7) "AI analyzing screenshot..." → "Analyzing screenshot…"; (8) redundant `_hasText` removed; (9) dead "partial match" branch → real signal (sources with confidence <0.5). `dart format` on touched files.
- Nits acknowledged (not fixed, low risk): EssayBlock `didUpdateWidget` (messages immutable — safe today); settings `_saveSettings` per-keystroke write churn (pre-existing); Detail status-bar overlay static (one of two states always suboptimal — acceptable); `_NavTab` lerp begin colors on mid-animation theme switch (cosmetic).
- Spec-adherence greps clean: 0 gradients/glass/glow, 0 auto_awesome/asterisk, 0 Inter, 0 fromSeed, 0 Hero, 0 shimmer, 0 emoji, 0 exclamation marks in UI copy, mono only in OCR, all 65 hexes in app_theme.dart, sentence case everywhere, no per-provider colors, evidence strip above answers, flat nav (no capsule/badge), accentDeep FilledButton.
- Regression: zero logic changes in services/models/config; only provider diff is the sanitized error + copy + benign `data` no-op. widget_test placeholder unaffected.

## Security Findings
- **Security reviewer: PASS-WITH-NOTES (no rotation needed, nothing blocking)**. Confirmed: services/models/config zero-diff; no secrets in tree or docs (pattern-grep clean, incl. WORKFLOWSTATE.md); fonts only in pubspec (no new deps); new-code surface clean (EvidenceStrip = local Image.file only, no network, no logging; no WebView; launchUrl scheme-validated); consent gate + local-only + deleteEverything intact; AndroidManifest untouched (allowBackup=false preserved, no new permissions); no new sensitive SharedPreferences keys (only hapticFeedback/onboarding_complete bools).
- Post-review hardening applied: process-error banner sanitized (key-in-URI leak class) — reviewer-flagged item closed.
- Pre-existing known limitations (out of scope, unchanged): API keys plaintext in SharedPreferences (mitigated by allowBackup=false; consider flutter_secure_storage as separate hardening); debug-only provider-body debugPrints in lam_service (assert-gated); settings key fields write on every keystroke.

## Test Results
- `flutter test` → **All tests passed!** 23/23 across 11 files (chat_single_provider, consent_findonline, consent_guard, delete_everything, extract_json ×5, lam_response ×4, no_fan_out ×3, screenshot_json, search ×3, web_lookup ×2, widget_test placeholder). Verified twice (compact + expanded reporter) — no flakiness.

## Lint Results
- `flutter analyze` → **No issues found!** (one info-level `curly_braces_in_flow_control_structures` at chat_screen.dart:657 found on first pass → fixed by implementor → clean re-run). `dart format` applied to touched files.

## Commit Message Draft
- Subject: `feat: warm paper recall redesign — flat paper UI, serif chat, fixes`
- Body: theme (hand-built ColorScheme, SiftColors ThemeExtension, SiftType serif/sans/mono, MotionTokens, bundled Source Serif 4 + JetBrains Mono OFL via LicenseRegistry, SiftMarkPainter asterisk-star) · screens (AppShell flat nav + deep-link, Home ask-bar + image cards, Chat two-phase + word-by-word streaming + persisted evidence strip, Settings checklist, Onboarding 3 beats, Detail honest action bar, History/Shopping wired) · robustness/security (sanitized process-error banner, tolerant sourceIds persistence, voice-clean copy) · verification (analyze clean, 23/23 tests, no new deps).
- Optional split (2 commits: redesign vs fixes) suggested — requires `git add -p` on app_theme/chat_message/screenshot_provider; single commit is honest and complete.

## Current Status
- **v3 "Warm Paper Recall" design: implemented + verified + security-audited.** All 5 design stages merged into the Consolidated Design Direction; implementor built it; reviewer PASS-WITH-NOTES (all routed fixes applied), security-reviewer PASS-WITH-NOTES (process-error sanitization closed the one flagged item), tester 23/23, linter clean. Working tree left **uncommitted** (commit is a user decision).
- Files: 21 modified + 2 new (assets/fonts ×5, lib/theme/motion_tokens.dart, lib/widgets/chat_atoms.dart) vs commit 419bf73. Untracked also: design_wip/.
- **NOTE (out-of-band)**: commit `419bf73 refactor(lam): inject http client, harden JSON parse, add test suite` (authored 11:29 today, repo-owner identity) appeared during the pipeline. It captured the PRE-EXISTING uncommitted state (LAM injectable-client refactor, JSON-parse hardening, 10 test files, WORKFLOWSTATE.md, an early snapshot of this DESIGNSTATE.md) — NOT the v3 redesign. Secret-scan of that commit is clean. The v3 redesign sits uncommitted on top of it.

## Next Agent
- **User decision**: commit the change (single commit or the 2-commit split). No further design-agent work required. Optional future hardening (out of scope): encrypted key storage (flutter_secure_storage), manifest permission pruning, exact-alarm runtime gating.
- Working tree: v2 code present (will be erased/replaced by implementor). Untracked: WORKFLOWSTATE.md, test/*.dart, design_wip/.

## Next Agent
- product
